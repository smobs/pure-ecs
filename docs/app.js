// Placeholder - will be generated by build process
// This file will be replaced with the compiled PureScript bundle

export const main = () => {
  console.log('Demo is building... Please wait for GitHub Actions to complete the build.');

  const canvas = document.getElementById('canvas');
  if (canvas) {
    const ctx = canvas.getContext('2d');

    // Draw loading message
    ctx.fillStyle = '#1a1a2e';
    ctx.fillRect(0, 0, 600, 400);

    ctx.fillStyle = '#ffffff';
    ctx.font = '20px monospace';
    ctx.textAlign = 'center';
    ctx.fillText('Building demo...', 300, 180);
    ctx.fillText('Check back in a few minutes!', 300, 220);
  }
};
